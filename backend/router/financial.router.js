const financialControllwr = require("../controllers/financial.controller");
const express = require("express");
const router = express.router();

//create a new financial
router.post("/", financialrouter.create

);

module.exports = router;